{## MR4 compiler integration test file #2

This file should onlw work together with test file #1.
#}


class MiddleType(BaseType)
  var Int num-mid
  user BaseType mid-base-ref
  user TopType mid-top-ref
  
  func dynamic meth1(copy Int n, user String s)
    sys.println(user "MiddleType.meth1")
  
  func dynamic meth2()
    sys.println(user "MiddleType.meth2")
    base.meth2()
  
  func dynamic meth4()
    sys.println(user "MiddleType.meth4")
  
  func dynamic meth5(copy Int n, user String s)
    sys.println(user "MiddleType.meth5")
