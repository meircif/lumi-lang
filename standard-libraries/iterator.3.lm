## Generic iterator dynamic interface


class Iterator{Generic}
  ## Return whether iteration has ended,
  ## `get-item()` can be used only if this returns false.
  func dynamic is-end():(copy Bool exist)
    raise
  
  ## Get current item, fail if not exists
  func dynamic get-item():(user Generic item)
    raise
  
  ## Move to next item
  func dynamic next()
    raise
